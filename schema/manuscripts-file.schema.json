{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nikilachniki.github.io/lassoDBData/schema/manuscripts-file.schema.json",
  "title": "Lasso-Werkverzeichnis, Datei manuscripts.json",
  "description": "Die Huelle um die Handschriften-Zeugnisse. manuscript.schema.json beschreibt ein einzelnes Zeugnis in items, nicht diese Datei als Ganzes, deshalb ein eigenes Schema dafuer.",
  "type": "object",
  "required": ["@context", "items"],
  "properties": {
    "$schema": { "type": "string" },
    "@context": { "type": "string" },
    "items": {
      "type": "array",
      "items": { "$ref": "manuscript.schema.json" }
    }
  }
}
